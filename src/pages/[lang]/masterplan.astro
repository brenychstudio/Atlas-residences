---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { LANGS, type Lang, isLang, t } from "../../content/languages";

export function getStaticPaths() {
  return LANGS.map((lang) => ({ params: { lang } }));
}

const langParam = Astro.params.lang ?? "en";
if (!isLang(langParam)) throw new Error("Invalid lang");
const lang = langParam as Lang;
---

<BaseLayout lang={lang} title={t(lang, "Masterplan | Atlas Residences", "Plan maestro | Atlas Residences") }>
  <section class="space-y-3">
    <h1 class="text-3xl tracking-tight">{t(lang, "Masterplan", "Plan maestro")}</h1>
    <p class="opacity-80">{t(lang, "Masterplan placeholder.", "Marcador de posición del plan maestro.")}</p>
  </section>
</BaseLayout>
